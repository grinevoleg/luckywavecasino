# Lucky Wave Chronicles: A Casino Story

Интерактивная визуальная новелла в жанре казино-нуар с элементами мини-игр и множественными концовками.

## Описание

"Lucky Wave Chronicles" - это история об агенте под прикрытием или талантливом мошеннике, который путешествует по самым роскошным казино мира, выполняя миссии. Игра сочетает элементы визуальной новеллы с мини-играми казино (блэкджек, слоты, покер), где успех в играх влияет на развитие сюжета.

## Особенности

- 📖 Интерактивный сюжет с множественными концовками
- 🎰 Мини-игры казино (блэкджек, слоты, покер)
- 🎨 Генерация изображений через Hugging Face MCP сервер
- 💾 Система сохранений через LocalStorage
- 💎 Система монетизации (ключи/билеты для глав и премиум-выборов)
- 🎭 Косметические наряды для персонажа

## Структура проекта

```
/
├── index.html                 # Главная страница
├── css/
│   ├── main.css              # Основные стили
│   ├── novel.css             # Стили визуальной новеллы
│   └── casino.css            # Стили мини-игр
├── js/
│   ├── main.js               # Точка входа
│   ├── novel-engine.js       # Движок визуальной новеллы
│   ├── story-data.js         # Данные сюжета
│   ├── casino-games.js       # Мини-игры казино
│   ├── save-system.js        # Система сохранений
│   ├── monetization.js       # Система монетизации
│   ├── hf-image-generator.js # Генератор изображений
│   └── mcp-bridge.js         # Мост для MCP сервера
├── assets/
│   ├── images/               # Сгенерированные изображения
│   ├── audio/                # Музыка и звуки
│   └── fonts/                # Шрифты
└── chapters/                 # Данные глав (JSON)
    └── chapter1.json
```

## Установка и запуск

1. Клонируйте репозиторий или скачайте файлы
2. Откройте `index.html` в браузере
3. Для тестирования с тестовыми ресурсами добавьте `?test=true` к URL

## Генерация изображений

Игра поддерживает два режима работы с изображениями:

1. **Локальные изображения (рекомендуется)** - все изображения генерируются заранее и сохраняются локально
2. **Генерация на лету** - изображения генерируются через MCP сервер при загрузке игры

### Генерация локальных изображений

Для генерации всех изображений заранее используйте MCP функции в Cursor IDE:

1. Откройте файл `generate-with-mcp.md` - там есть готовые промпты для каждого изображения
2. Используйте MCP функцию `gr1_flux1_schnell_infer` для генерации каждого изображения
3. Сохраните результаты в соответствующие папки:
   - Фоны → `assets/images/backgrounds/`
   - Персонажи → `assets/images/characters/`
4. Обновите `assets/images/manifest.json` с путями к изображениям

После генерации игра автоматически будет использовать локальные файлы.

Подробные инструкции см. в `GENERATE_IMAGES.md` и `generate-with-mcp.md`.

### Интеграция с Hugging Face MCP сервером

Для использования реальной генерации изображений через HF MCP сервер необходимо настроить серверный прокси.

### Вариант 1: Использование placeholder (по умолчанию)

По умолчанию игра использует placeholder изображения, созданные на canvas. Это позволяет тестировать игру без настройки сервера.

### Вариант 2: Серверный прокси для MCP

Создайте серверный прокси, который будет вызывать MCP функции. Пример на Node.js:

```javascript
// server.js
const express = require('express');
const app = express();
app.use(express.json());

// Здесь должен быть код для вызова MCP сервера
// Например, через MCP SDK или прямой вызов API

app.post('/generate-image', async (req, res) => {
    try {
        // Вызов MCP функции gr1_flux1_schnell_infer
        const result = await mcpClient.call('gr1_flux1_schnell_infer', req.body);
        res.json({ imageUrl: result.imageUrl });
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});

app.listen(3000, () => {
    console.log('MCP Proxy server running on http://localhost:3000');
});
```

Затем в `index.html` добавьте перед закрывающим тегом `</body>`:

```html
<script>
    window.MCP_SERVER_URL = 'http://localhost:3000';
</script>
```

### Вариант 3: Прямая интеграция через Cursor IDE

Если игра запускается в среде Cursor IDE с настроенным MCP сервером, можно использовать прямой вызов через специальный интерфейс.

## Использование

### Начало игры

1. Нажмите "Новая игра" в главном меню
2. Следуйте диалогам и делайте выборы
3. Играйте в мини-игры для продвижения по сюжету

### Сохранение и загрузка

- Нажмите кнопку меню (☰) в правом верхнем углу
- Выберите "Сохранить" или "Загрузить"
- Выберите слот для сохранения

### Система монетизации

- **Ключи** - используются для открытия новых глав
- **Билеты** - используются для премиум-выборов
- Для тестирования добавьте `?test=true` к URL для получения тестовых ресурсов

## Разработка

### Добавление новой главы

1. Создайте файл `chapters/chapter2.json` с данными главы
2. Добавьте главу в `story-data.js`:

```javascript
getChapter2() {
    return {
        chapterId: "chapter2",
        title: "Вторая волна",
        scenes: [...]
    };
}
```

3. Загрузите главу в `loadChapters()`:

```javascript
this.chapters = {
    chapter1: this.getChapter1(),
    chapter2: this.getChapter2()
};
```

### Добавление мини-игры

1. Добавьте логику игры в `casino-games.js`
2. Добавьте стили в `casino.css`
3. Интегрируйте в сцену через поле `minigame`

### Генерация изображений

Изображения генерируются автоматически при загрузке сцены. Для предзагрузки используйте:

```javascript
await imageGenerator.preloadChapterImages(chapterData);
```

## Технологии

- HTML5
- CSS3
- Vanilla JavaScript (ES6+)
- LocalStorage API
- Hugging Face MCP Server (опционально)

## Лицензия

Проект создан для демонстрационных целей.

## Контакты

Для вопросов и предложений создайте issue в репозитории.

